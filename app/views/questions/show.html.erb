<div class="container">
  <h2 class="title-lg mb">Вопрос <%= @question.id %></h2>
  <p class="mt-sm">
    Все вопросы <%= link_to "@#{@question.user.nickname}", user_path(@question.user) %>
  </p>
</div>

<div class="container mt-sm">
  <%= render @question %>
</div>
